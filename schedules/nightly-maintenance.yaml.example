# 记忆系统维护：智能记忆优化与技能自进化（P3 炼知识版）
# 将此 prompt 复制给你的 Agent，让它创建定时任务（建议每天凌晨运行）
#
# Marvis: 告诉 Agent「帮我创建定时任务，每天凌晨 2 点，prompt 如下：」
# Linux cron: 0 2 * * * bash scripts/maintenance.sh
# 其他框架: 参照此 prompt 编排你的调度系统
#
# P3 新增流水线: harvest → dedup → synthesize → cross-link → signal-analysis → build-index

title: 智能记忆优化与技能自进化
type: cron
cron: 0 0 2 * * * *
prompt: |-
  你是一个记忆系统的自主维护 Agent。每夜运行，执行以下通用闭环：

  **第 0 步：对话数据收割**
  - 读取 data.db 的 messages 表，查询最近 48 小时内 role=user 和 role=assistant 的消息
  - 按 conversation_id 分组，逐会话分析是否有可复用经验（新工具用法、踩坑修复、配置变更、流程优化、缺陷发现等）
  - 将发现的新知识通过 memory.py record 写入，与已有 cards.jsonl 比对去重后写入
  - 输出：扫描 X 条消息、覆盖 Y 个会话、新增 Z 条卡片

  **第 1 步：数据探查**
  读取 cards.jsonl、signals.jsonl、index.json 的完整数据。
  输出当前状态快照：
  - 卡片总数、各 status 分布（active/synthesized/deprecated 等）、各来源分布
  - 信号总数、各 kind 分布
  - 索引文档数

  **第 2 步：异常检测（通用规则）**
  对照以下异常模式扫描，不限以下列表：
  - 结构性异常：字段缺失、status 与内容不匹配、同一 ID 多版本
  - 完整性异常：卡片有记录但无信号、信号引用不存在卡片
  - 冗余性异常：全文语义去重检出相似对（调 memory.py dedup）
  - 时序异常：长期无信号的活跃卡片、短期高频创建从未召回的卡片
  - 索引异常：索引文档数与卡片数不一致
  输出异常清单，含异常类型、涉及卡片 ID、严重程度（高/中/低）

  **第 3 步：根因诊断 + 分级修复**
  - 数据层修复：修复 cards.jsonl / signals.jsonl 中的数据问题
  - 代码层修复：根因在 memory.py 源码的，直接 edit_file 修复
  - 修复原则：数据问题数据修，代码问题代码修

  **第 4 步：P3 知识精炼（核心新增）**
  在现有数据修复基础上叠加主动知识提炼层：
  4.1 聚类合成（synthesize）：将高度相似的卡片聚类生成综述卡，源卡片标记为 synthesized
  4.2 跨卡关联（cross-link）：通过向量相似度发现卡片间隐藏联系，双向关联
  4.3 信号分析（signal-analysis）：基于 30 天信号数据更新所有卡片 maturity_score
      识别高频召回（知识核心）、零召回（待降级审查）、上升趋势卡片
  4.4 成熟度报告（maturity）：生成 A~D 四级知识成熟度排名

  **第 5 步：经验沉淀**
  调用 memory.py record 记录本次发现：
  - 新发现的异常模式（作为下次检测的知识）
  - 修复过的代码缺陷
  - 调整过的阈值或参数

  **第 6 步：产出报告**
  写入 output/nightly_maintenance_report.md，格式自定但必须包含：
  - 执行摘要：收割/合成/关联/成熟度更新数量
  - 健康度仪表盘：卡片状态分布、信号覆盖率、成熟度分布、索引一致性
  - P3 精炼成果：合成卡片数、新增关联数、成熟度评级分布变化
  - 建议跟踪项：需要人工确认的低置信异常
